{% set name = "Orange3-Geo" %}
{% set version = "0.2.4" %}
{% set sha256 = "68c559aac716f7519a03757165f5227e412eb001e23bbf5a0887ee5922bf5e2b" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  script: python setup.py install --single-version-externally-managed --record record.txt
  skip: True  # [py2k]

requirements:
  build:
    - python
    - setuptools
  run:
    - python
    - orange3
    - scikit-learn
    - pandas
    - scipy >=0.17
    - shapely
    - simplejson

test:
  imports:
    - orangecontrib.geo

about:
  home: http://github.com/biolab/orange3-geo
  license: GPL-3.0
  license_family: GPL
  summary: 'Orange add-on for dealing with geography and geo-location'
  dev_url: http://github.com/biolab/orange3-geo

extra:
  recipe-maintainers:
    - kernc
    - astaric
